import Layout from "../../components/Layout";

<Layout>
# Deploying

To build a project based on Inn.ts into an optimized, static site — simply run `yarn build`. If you are not using the Inn.ts template, this is equivalent to `tsc && vite build`.

</Layout>
